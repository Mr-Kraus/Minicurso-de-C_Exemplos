#include <stdio.h>

int main() {
    int a = 1, b = 0;

    printf("a && b = %d\n", a && b);  // E lógico (AND)
    printf("a || b = %d\n", a || b);  // OU lógico (OR)
    printf("!a = %d\n", !a);          // NÃO lógico (NOT)

    return 0;
}
